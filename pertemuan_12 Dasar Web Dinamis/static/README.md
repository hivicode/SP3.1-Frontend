# Static Assets

Folder ini menampung aset statis untuk aplikasi Flask pertemuan 12.

- Simpan gambar produk di `image/` agar bisa diakses melalui `/static/image/<filename>`.
- Tambahkan CSS/JS tambahan di sini bila dibutuhkan oleh template Bootstrap.
